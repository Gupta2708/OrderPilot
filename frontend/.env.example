# Frontend configuration. Copy to .env.local only if the API is not on the default host.
# The browser calls this directly, so it must be reachable from the browser.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
